{% extends "::back.html.twig" %}
{% block recharge %}
<style>
.rower{
  display:flex;
  flex-direction:row;
}
</style>
<div class="row rower">
    <div class="col-lg-6">
            <div class="card">
              <div class="card-header">
              <p style="font-size:32px;"> Information sur {{reservation.client.nomclient}}</p>
              </div>
              <div class="card-body">
                  <h4 class="card-title"></h4>
                       <p class="card-text">
                            <p style="font-size:16px;"><b>Tel:</b>{{reservation.client.telclient}}</p>
                            <p style="font-size:16px;"><b>Adresse:</b>{{reservation.client.adresseclient}} </p>       
                             <p style="font-size:16px;"> <b>Email:</b>{{reservation.client.emailclient}}</p>
                             
                        </p>
              </div>            
            </div>
          </div>
                  <div class="col-lg-6">
                    <div class="card">
                        <div class="card-header">
                        <p style="font-size:32px;"> Information sur le bien</p>
                        </div>
                        <div class="card-body">
                            <h4 class="card-title"></h4>
                                <p class="card-text">
                                      <p style="font-size:16px;"> <b> Prix_location:</b>{{reservation.bien.prix_loc}}</p>
                                      <p style="font-size:16px;"> <b>Localité:</b>{{reservation.bien.localite.libelleloca}}</p>
                                      <p style="font-size:16px;"> <b>Typebien:</b>{{reservation.bien.typebien.libelletype}} </p> 
                                       <p style="font-size:16px;"> <b>Description:</b>{{reservation.bien.description}} </p>                         
                                </p>
                             </div>            
                     </div>
               </div>
  </div>
                    <div class="row " style="margin-top:50px;">
                        <div class="col-lg-12 rower">
                         {% for image  in reservation.bien.images %}
                           
                          <img src="{{ asset('image/')}}{{ image.image }}" alt="Image" class="img-responsive" style="width:250px;height:200px;padding-left:10px;">

				                {% endfor %}
                         </div>
                    </div><br>
                      <div class="row" style="margin-top:50px;">
                        <div class="col-lg-12">
                            <a href="{{path('saveContrat', {'id':reservation.id})}}" class="btn btn-primary "style="margin-left:90%;">Valider</a>
                        </div>
                      </div>
                             {# <a href="{{ asset('image/')}}{{ reservation.bien.images[0].image }}" class="fh5co-card-item image-popup">	
		                             <div id="myCarousel" class="carousel slide" data-ride="carousel">
                               
                                     <div class="carousel-inner" role="listbox">
                                         <div class="item active">
                                               <img src="{{ asset('image/')}}{{ reservation.bien.images[0].image }}" alt="Image">

                                       </div>

                                               {% for image  in reservation.bien.images %}
                                                  <div class="item">      
                                                      <img src="{{ asset('image/')}}{{ image.image }}" alt="Image" class="img-responsive" >
                                                  </div>
                                      
                                                {% endfor %}
                                       </div>
                                 </div>
                                           <a class="left carousel-control" href="#myCarousel" role="button" data-slide="prev">
                                            <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
                                            <span class="sr-only">Previous</span>
                                          </a>
                                            <a class="right carousel-control" href="#myCarousel" role="button" data-slide="next">
                                              <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
                                              <span class="sr-only">Next</span>
                                          </a>
                            </div>
            	              	</a> #}
                          
   {# 
  
         {# <b>Nom du client:</b>{{reservation.client.nomclient}}
         <b>TelClient:</b>{{reservation.client.telclient}}
         <b>AdresseClient:</b>{{reservation.client.adresseclient}}        
          <b>Email:</b>{{reservation.client.emailclient}} #}
      
        {# <b> Prix_location:</b>{{reservation.bien.prix_loc}}
         <b>Localité:</b>{{reservation.bien.localite.libelleloca}}
         <b>Typebien:</b>{{reservation.bien.typebien.libelletype}}
        
        </td>
           
              </tr>
              </table>
       </div>
        <div class="col-lg-2">
         <table class="table" id="myTable" >
  <thead>
    <tr>
      <th>Images</th>     
      
    </tr>
  </thead>
  {%for image in reservation.bien.images %}
     <tr>    
         <td><img  src="{{ asset('image/')}}{{image.image }}" alt="" style="width:100px;"></td>
          </tr>
          {% endfor %}
       </table>
       </div>
      
       <div class="col-lg-2"> #}
   {# <form method="post"  style="display:flex;display-flex:row;list-style:none;"> #}
      {# <input type="submit" name="retour" class="btn btn-primary " value="Retour"> #}
      
 {# </form> #}
   
{% endblock %}
